/// <reference types="node" />
import { type Pooled } from '@php-wasm/universal';
import type { PlaygroundCliBlueprintV2Worker } from './worker-thread-v2';
import type { MessagePort as NodeMessagePort } from 'worker_threads';
import { type PlaygroundCliWorker, type RunCLIArgs, type SpawnedWorker, type WorkerType } from '../run-cli';
import type { CLIOutput } from '../cli-output';
/**
 * Boots Playground CLI workers using the native TypeScript Blueprint v2
 * compiler and step runtime.
 */
export declare class BlueprintsV2Handler {
    private siteUrl;
    private args;
    private cliOutput;
    private wordpressInstallModePromise?;
    constructor(args: RunCLIArgs, options: {
        siteUrl: string;
        cliOutput: CLIOutput;
    });
    getWorkerType(): WorkerType;
    bootWordPress(playground: Pooled<PlaygroundCliWorker>, workerPostInstallMountsPort: NodeMessagePort): Promise<Pooled<PlaygroundCliWorker>>;
    bootRequestHandler({ worker, fileLockManagerPort, nativeInternalDirPath, }: {
        worker: SpawnedWorker;
        fileLockManagerPort: NodeMessagePort;
        nativeInternalDirPath: string;
    }): Promise<import("@php-wasm/universal").RemoteAPI<PlaygroundCliBlueprintV2Worker>>;
    compileInputBlueprint(additionalBlueprintSteps: any[]): Promise<{
        declaration: import("packages/playground/blueprints/src/lib/v2/wep-1-blueprint-v2-schema/appendix-A-blueprint-v2-schema").V2Schema.BlueprintV2;
        runtime: import("@wp-playground/blueprints").RuntimeConfiguration;
        applicationOptions?: {
            'wordpress-playground': {
                landingPage?: string | undefined;
                login?: boolean | {
                    username: string;
                    password: string;
                } | undefined;
                networkAccess?: boolean | undefined;
                loadPhpExtensions?: "intl"[] | undefined;
            };
        } | undefined;
        plan: import("packages/playground/blueprints/src/lib/v2/compile").BlueprintV2ExecutionPlan;
        steps: import("packages/playground/blueprints/src/lib/v2/compile").BlueprintV2StepPlan;
        unsupportedPlan: import("packages/playground/blueprints/src/lib/v2/compile").BlueprintV2ExecutionPlan;
        run: (playground: import("@php-wasm/universal").UniversalPHP) => Promise<void>;
    }>;
    /**
     * Resolves the worker install mode, including migrated v1 PHP-only sites.
     */
    private getWordPressInstallMode;
    /**
     * Returns a v2 declaration after applying CLI-level defaults.
     *
     * Bundled Blueprints keep a `streamBundledFile` callback because v2 lowering
     * still needs the original archive to resolve bundled resources.
     */
    private getEffectiveBlueprint;
}
